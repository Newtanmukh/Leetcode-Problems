   vector<int>ans;
    
    void dfs(Node* root)
{
    if(root==NULL)
        return;
     
    for(auto x:root->children)
    {
        dfs(x);
    }
        ans.push_back(root->val);
       
    
}
    vector<int> postorder(Node* root) {
        dfs(root);
        return ans;
    }