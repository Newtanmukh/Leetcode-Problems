int maxSumPairWithDifferenceLessThanK(int a[], int n, int k)
   {
       int sum=0;
       int i=n-1;
       sort(a,a+n);
       while(i>=1)
       {
           if(a[i]-a[i-1]<k)
           sum=sum+a[i]+a[i-1],i=i-2;
           else
           i--;
       }
       return sum;
   }