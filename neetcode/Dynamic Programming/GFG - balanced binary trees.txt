long long int countBT(int h) { 
        long long int dp[h + 1];
    //base cases
    
    if(h==0||h==1)
    return h;
    
    
    dp[0] = dp[1] = 1;
    for(int i = 2; i <= h; i++) {
        dp[i] = (dp[i - 1] * ((2 * dp [i - 2]) + dp[i - 1])) % 1000000007;
    }
    return dp[h];
    }